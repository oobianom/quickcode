---
title: "quickcode: R package that is just my compilation of useful functions across various processes"
author: "O.N Obianom"
date: "`r Sys.Date()`"
output: rmarkdown::html_vignette
vignette: >
  %\VignetteIndexEntry{quickcode: R package that is just my compilation of useful functions across various processes}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
---

```{r setup, include = FALSE}
knitr::opts_chunk$set(
  collapse = TRUE,
  comment = "#>"
)
```


### A compilation of some useful functions to simply R programming, keep the console neat, and save memory usage 

## Install

```
devtools::install_github("oobianom/quickcode")

install.packages("quickcode") # when published

```

## List of many cool functions to use

```
clean
grapes-nin-grapes
not.numeric
not.null
not.empty
not.vector
not.integer
not.environment
not.data
not.logical
libraryAll
g.mean
sd.gm
cv.gm
vector_push
list_push
inc
data_push
vector_shuffle
data_shuffle
refresh
insertInText
add.header
add.snippet.clear
header.rmd
sample_by_column
duplicate
number
init

```

Check out some examples at <u>quickcode.obi.obianom.com</u>
